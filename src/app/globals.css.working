@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --background: white;
  --foreground: black;
}

.dark {
  --background: black;
  --foreground: white;
}

.font-patua {
  font-family: 'Patua One', cursive;
}

.font-montserrat {
  font-family: 'Montserrat', sans-serif;
}
